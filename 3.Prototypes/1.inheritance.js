// classical inheritance vs protypical inheritance
// JS has no class, so only protypical inheritance
// the class from where we inherit , callled base/super/parent class
// the class which inherits properties , called derived/sub/child class
// if circle is child and shape is parent , we call circle is a shape
